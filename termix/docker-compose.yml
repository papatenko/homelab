services:
  termix:
    image: ghcr.io/lukegus/termix:latest
    container_name: termix
    restart: unless-stopped
    network_mode: host
    volumes:
      - ${DATA_DIR:-/opt/stacks/termix}:/app/data
    environment:
      PORT: "8080"
      ENABLE_TELEMETRY: "false"

# Uptime Kuma stack variables (safe placeholders only)
# Keep the bind address on loopback or a private LAN/VPN interface; never use a public interface.
KUMA_BIND_ADDRESS=127.0.0.1
KUMA_PORT=3001
KUMA_DATA_DIR=/opt/stacks/uptime-kuma
TZ=UTC

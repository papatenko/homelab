services:
  canvas-todoist:
    build: .
    container_name: canvas-todoist
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=${TZ}
    volumes:
      - ${DATA_DIR:-/opt/stacks/canvas-todoist}/data:/data

# Native image-build overlay. Do not select this file for Portainer Git stacks.
# On the Services host, build the locally tagged image before a Portainer deploy:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml build tokentracker
services:
  tokentracker:
    build:
      context: .
      dockerfile: Dockerfile
